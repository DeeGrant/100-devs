let Board = {
    spaces: ['x', 'o', '', '', '', ''],
    playerTurn: true, // true = x, false = 0
    // number of turns?
    calculateForWin() {
        // calculate if there is win or draw
        // output message
    }
}
